Ce pack garde l'index original comme base.
Les pages judge/public/timekeeper/secours lisent localStorage `delta_admin_payload` et `delta_live_payload`.
Sans intégration plus poussée, elles affichent une démo exploitable.
